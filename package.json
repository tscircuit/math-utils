{
  "name": "@tscircuit/math-utils",
  "main": "dist/index.js",
  "version": "0.0.1",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup-node src/index.ts --format esm --dts --sourcemap"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsup": "^8.2.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
